# Web Server

## Usage

Run TypeScript transpilation:

```sh
$ npm run build
```

Run the web server:

```sh
$ npm run start
```

## Development

Start TypeScript transpilation in watch mode:

```sh
$ npm run build:watch
```

In a separate terminal instance, start the web server in watch mode:

```sh
$ npm run dev
```
